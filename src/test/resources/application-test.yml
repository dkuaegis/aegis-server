spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:tc:postgresql:18.3-trixie://test/testdb
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
  session:
    timeout: 30m

exception:
  log-only-aegis-stack-trace: false

email-restriction:
  enabled: true
  admin-email: dankook.aegis@gmail.com

oauth2-login:
  default-redirect-uri: http://localhost:5173

internal-api-key:
  tx-track-api: test-api-key

discord:
  token: test-discord-token
  guild-id: test-guild-id
  alarm-channel-id: test-alarm-channel-id
  complete-role-id: test-complete-role-id
