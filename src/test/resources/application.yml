spring:
  application:
    name: aegis-server
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
  session:
    timeout: 0
